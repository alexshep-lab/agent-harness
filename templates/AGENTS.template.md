> **TL;DR (EN).** The template every new project folder starts from: what the
> project is, folder map, conventions, current status, and an explicit
> "do not touch" list. Russian — the workspace's working language.

# AGENTS.md — <Название проекта/продукта>

Этот файл — для AI-агентов (Claude Code, Codex, Cursor, Gemini, NotebookLM).
Любой свежий агент, прочитав AGENTS.md, должен понимать контекст без
необходимости лезть в чат-историю.

## Что это

<1–2 абзаца. Что за проект, для кого, в какой стадии. Что важно знать с порога.>

## Структура папки

```
<схема каталогов с описанием что внутри>
```

- `src/` — <что там>
- `docs/` — <что там>
- `data/` — <что там>
- `_Архив/` — старое, **не трогать без явного запроса**

## Соглашения

- **Язык кода:** <Python 3.12 / TypeScript / ...>
- **Язык документов:** русский (если не указано иное)
- **Где `.env`:** `<путь>` (gitignored; секреты — в менеджере секретов)
- **Линтер / форматер:** `<команда>`
- **Тесты:** `<команда запуска>`

## Текущее состояние (обновляется еженедельно)

<3–5 строк: над чем работаю прямо сейчас, какие открытые вопросы, что готово,
что блокировано.>

## Активные направления

- <линия работы 1 — статус>
- <линия работы 2 — статус>

## Связанные ресурсы

- **Todoist-проект:** <имя>
- **Сервер:** <домен>
- **Репозиторий:** <URL>
- **Папки-соседи:** <пути в файловой системе, с которыми связан этот проект>

## Что не трогать

- `_Архив/` — старое, для контекста только
- `legacy_*.py` — устаревший код
- <…>

## Подсказки для агента

- Перед изменениями — прочитать `docs/architecture.md` (если есть)
- При работе с базой данных — НЕ дропать таблицы без явного подтверждения
- При работе с прод-сервером — все правки через PR/коммит, без `ssh prod` напрямую
- <…>

---

*Шаблон — версия 1. Адаптировать под конкретный проект, удалять неактуальные секции.*
